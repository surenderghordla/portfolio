document.querySelectorAll('.js-text').forEach(function(event){
    new Typed(event,{
    strings:['Frontend Developer','Web Developer'],
    typeSpeed:100,
    backSpeed:100,
    backDelay:1000,
    loop:true
    })
});